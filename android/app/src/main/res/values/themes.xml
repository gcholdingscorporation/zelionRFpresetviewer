<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- No action bar: the page draws its own header, as it does everywhere else. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#3d3f3e</item>
    </style>
</resources>
